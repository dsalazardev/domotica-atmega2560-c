#include <avr/io.h>
#include <avr/interrupt.h>
#include <util/delay.h>

#include "Seguridad.h"

Seguridad seguridad;

void setup() {
    for (int i = 0; i < 10; ++i) {

    }
}

void loop() {
    _delay_ms(50);
}
